FROM python:3.11-slim

WORKDIR /app
COPY requirements-api.txt .
RUN pip install --no-cache-dir -r requirements-api.txt

COPY api/ ./api/
COPY .env.example .env

EXPOSE 8000
CMD ["uvicorn", "api.main:app", "--host", "::", "--port", "8000"]
